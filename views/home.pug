extends layouts/main
include mixin/videoBlock

block content
    h1 #{pageTitle}
    .videos
        each item in videos
            +videoBlock({
                videoFile: item["videoFile"],
                title: item["title"],
                views: item["views"]
            })